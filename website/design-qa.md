# Hemusci Skills 设计与功能验收记录

- 检查日期：2026-08-17
- 检查页面：`/skills/`
- 桌面视口：1440 × 900 CSS px
- 移动视口：390 × 844 CSS px
- 数据基准：5 个技能、228 条期刊记录

## 本次改版

- 首页重新设计为土壤科学专业技能站，采用纸张白、深绿和陶土色构成的克制型视觉系统，不使用渐变、装饰性气泡或彩色底纹堆叠。
- `soil-all-writing` v1 设为主技能和默认安装项，集中展示写作翻译、术语审校、证据边界、图表分析、模板格式与多模型接入能力。
- 原有四个专项技能改为同一专业矩阵中的独立模块，保留版本号、适用范围、技能说明和期刊资料库入口。
- 首页信息更新为 5 个技能、29 类科学写作文体、13 个学科模块、228 条期刊格式资料和 162 项写作技能确定性回归测试。
- 安装区扩展为 5 个技能与 6 个智能体的组合选择器；通用模型接入说明覆盖 OpenAI、Claude、Gemini、DeepSeek、Qwen、Mistral、Cohere、Amazon Bedrock、Ollama 和自定义接口。
- 页脚保留同源匿名访问统计；本地预览无法访问正式统计接口时显示离线状态，不影响正文和交互。

## 结构与静态检查

- 技能页可访问链接：22；交互按钮：12；技能标签：5；智能体标签：6。
- 页面 ID 无重复，站内锚点均有对应目标。
- 内嵌 JavaScript 通过 `node --check`。
- HTML 结构解析通过；主要区域具备 `banner`、`navigation`、`main`、`region`、`article` 和 `contentinfo` 语义。
- `soil-all-writing` 技能说明和 v1 发布包链接可访问；期刊资料库详情页本地返回 200。

## 桌面端检查

- 1440 × 900 下浏览器可用宽度为 1425 px，文档滚动宽度同为 1425 px，无横向溢出。
- 首屏主标题、简介、操作按钮、五技能发布索引和五项统计完整显示，文字未被裁切。
- 技能矩阵采用主技能双栏版式；能力条目、协议适配、说明链接和下载链接均处于容器内。
- 安装区左右分栏清楚，技能与智能体选择、命令区和通用接入说明未发生重叠。

## 移动端检查

- 390 × 844 下浏览器可用宽度与文档滚动宽度均为 375 px，无横向溢出。
- 移动端导航保留品牌和 GitHub 入口；主标题、按钮、发布索引、技能卡及安装组件按单列排列。
- `soil-all-writing` 主技能的功能标签、下载入口和能力条目均在窄屏内正常换行。
- 五个技能按钮和六个智能体按钮改为单列，长技能名与命令文本不撑破容器。

## 交互与可访问性检查

- 技能与智能体选择器使用 `tablist` / `tab` 语义，并同步 `aria-selected` 与 `tabindex`。
- 切换至“期刊格式审查 + Gemini CLI”后，安装命令同步更新为 `soil-journal-format-review --agent gemini-cli`。
- 键盘方向键切换逻辑保留；复制反馈使用 `aria-live`；键盘焦点具有可见轮廓。
- 页面支持 `prefers-reduced-motion`。
- 桌面端和移动端浏览器控制台 error / warning 均为 0。

## 正式环境复核

- 正式域名首页已显示 `soil-all-writing`、`05 skills`、`29`、`13`、`228` 和 `162`。
- 公网浏览器识别到 5 个技能标签和 6 个智能体标签；切换至“土壤试验方法 + Claude Code”后，命令同步更新为 `soil-methods-consultant --agent claude-code`。
- `/skills/soil-journal-format-review/` 保持 228 张期刊卡；证据 CSV 为 229 行，其中 1 行为表头。
- `/api/visits` 正常返回累计访问、当日访问、当日访客、运行天数和近 7 日趋势，页脚状态为 `Live / Visitor pulse`。
- 公网首页 SHA-256 为 `4e59b2b222da482872fd1e8fd653f18248c695a2454f66068d16c1e6500cb4ec`，与本地发布文件及服务器文件一致。
- Nginx 配置检查通过；覆盖前版本已备份到服务器 `/var/backups/hemusci-skills/20260817-003133/`。
- 正式域名浏览器控制台 error / warning 为 0。

## 结论

本地静态检查、桌面端渲染、移动端渲染、安装命令联动、正式环境部署和公网复核均通过。

final result: passed
